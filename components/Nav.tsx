import React from 'react';

const Nav = () => {
  return (
    <nav
      style={{
        background: 'RebeccaPurple',
        width: '100%',
        height: '100px',
        color: 'white',
        textAlign: 'center',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: '24px',
      }}>
      Nav (Remote 3001)
    </nav>
  );
};

export default Nav;